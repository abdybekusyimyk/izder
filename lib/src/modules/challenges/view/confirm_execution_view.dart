import 'package:flutter/material.dart';

class ConfirmExecutionView extends StatelessWidget {
  const ConfirmExecutionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Confirm Execution'),
      ),
      body: Center(
        child: Text('Confirm Execution'),
      ),
    );
  }
}
