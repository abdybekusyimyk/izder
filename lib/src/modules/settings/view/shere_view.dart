import 'package:flutter/material.dart';

class ShereView extends StatelessWidget {
  const ShereView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Share'),
      ),
      body: Center(
        child: Text('Share'),
      ),
    );
  }
}
